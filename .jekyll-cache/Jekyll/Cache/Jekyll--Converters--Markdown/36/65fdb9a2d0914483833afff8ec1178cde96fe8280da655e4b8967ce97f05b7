I"�<p>So I wrote a little bash script this morning to make creating a new jekyll post easy for my blog.</p>

<p>~~~
array=( $@ )
len=${#array[@]}
_first=$1
_last=${array[$len-1]}
_rest=${array[@]:1:$len-2}
_filename=$( printf “%s-%s” “$(date +%F)” “$_first” )
_postname=$( printf “%s %s” “$_first” “$_rest” )</p>

<p>echo $_rest</p>

<p>for var in $_rest 
do
    _filename+=”-“
    _filename+=$var
done</p>

<h2 id="cat-eof--_posts_filenamemd">cat «EOF &gt; _posts/$_filename.md</h2>
<p>layout: post
title: $<em>postname
comments: true
date:   $(date +%F</em>%T) 
categories: $_last
tags: [’’]
image:
description:
—</p>

:ET